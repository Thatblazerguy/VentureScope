# VentureScope Frontend

React + Vite + Tailwind frontend for the VentureScope workspace.

## Environment
- `VITE_API_URL` points to the backend URL.

## Scripts
- `npm run dev`
- `npm run build`
- `npm run preview`